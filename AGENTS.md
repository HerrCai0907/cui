# Agent Instructions

- After making frontend changes, verify the result at `http://localhost:5173/`.
- If port `5173` is already in use, treat that as an existing background dev server and do not start a duplicate server.
